<Page x:Class="_222_Goman_WPF_Project.Pages.DiagrammPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_222_Goman_WPF_Project.Pages"
      xmlns:charts="clr-namespace:System.Windows.Forms.DataVisualization.Charting;assembly=System.Windows.Forms.DataVisualization"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" 
      Title="DiagrammPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="30, 0, 30, 0" Orientation="Horizontal" VerticalAlignment="Center">
            <Label Content="Пользователь" VerticalAlignment="Center"/>
            <ComboBox x:Name="CmbBxUsers" DisplayMemberPath="FIO"  SelectedValuePath="ID" SelectedValue="{Binding Path=Users.name}" Width="150" SelectionChanged="UpdateChart"/>
            <Label Content="Тип диаграммы" VerticalAlignment="Center"/>
            <ComboBox x:Name="CmbBxDiagramm" SelectedValuePath="sss" SelectedValue="{Binding Path=sss}" Width="150" SelectionChanged="UpdateChart"/>
            <Button x:Name="ButtonExportWord" Width="100" Content="Word"/>
            <Button x:Name="ButtonExportExcel" Width="100" Content="Excel"/>
        </StackPanel>
        
        
        
        <Grid ShowGridLines="True" Grid.Row="1" Margin="100, 10, 100, 0">
            <WindowsFormsHost Height="300" Grid.Row="1" VerticalAlignment="Top" Width="400" RenderTransformOrigin="0.544,1.453">
                <charts:Chart x:Name="ChartPayments">
                    <charts:Chart.Legends>
                        <charts:Legend>

                        </charts:Legend>
                    </charts:Chart.Legends>
                </charts:Chart>
            </WindowsFormsHost>
        </Grid>
    </Grid>
</Page>
